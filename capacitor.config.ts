import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agora.reparflash',
  appName: 'QualiRéparFlash',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
